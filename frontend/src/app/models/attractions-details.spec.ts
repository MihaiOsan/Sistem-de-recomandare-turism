import { AttractionsDetails } from './attractions-details';

describe('AttractionsDetails', () => {
  it('should create an instance', () => {
    expect(new AttractionsDetails()).toBeTruthy();
  });
});
